# Dependencies
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Source files - we need these for building, but we'll deploy from dist/
# src/ - commented out because we need src files for GitHub Actions build

# Keep scripts folder for build process
!scripts/

# Keep source files for build process
!src/

# Development files
.DS_Store
*.log
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# IDE files
.vscode/
.idea/
*.swp
*.swo

# Temporary files
*.tmp
*.temp

# Ignore dist folder - GitHub Actions will build it in the cloud
dist/

# Keep manifest.xml in root for development
!manifest.xml

# Keep version.json for tracking
!version.json

# Keep README and other docs
!README.md
!*.md

# Keep assets folder
!assets/

# Ignore the SqExcelWeb subfolder (it's a separate project)
SqExcelWeb/
